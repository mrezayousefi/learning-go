package exercise

import "fmt"

func twofer(name string) string {
	if name == "" {
		return "One for you, one for me."
	}
	return fmt.Sprintf("One for %s, one for me.", name)
}

// The main function is not part of the twofer package, but is included here for testing purposes.
// It can be removed when using the package in a larger application.

// main function to demonstrate the twofer functionality

func main() {
	fmt.Println("Buy one bread, get two breads")
	var count string
	func(persion) {

	}

}
